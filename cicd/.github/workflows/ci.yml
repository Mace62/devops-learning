name: CoderCo CI/CD Workflow

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout code
          uses: actions/checkout@v3
        - name: We are testing this workflow
          run: echo Hello everyone!
          
            